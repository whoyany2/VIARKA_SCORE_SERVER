document.querySelectorAll('[data-filter]').forEach((button)=>button.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach((item)=>item.classList.remove('active'));button.classList.add('active');document.querySelectorAll('[data-period]').forEach((row)=>{const mode=button.dataset.filter;row.style.display=mode==='all'||row.dataset.period===mode?'table-row':'none'})}));
document.querySelectorAll('[data-auth-tab]').forEach((button)=>button.addEventListener('click',()=>{document.querySelectorAll('[data-auth-tab]').forEach((item)=>item.classList.toggle('active',item===button));document.querySelectorAll('[data-auth-panel]').forEach((panel)=>panel.hidden=panel.dataset.authPanel!==button.dataset.authTab)}));
const canvas=document.getElementById('scoreChart');
if(canvas){const values=JSON.parse(canvas.dataset.values||'[]');if(values.length){const ratio=window.devicePixelRatio||1,box=canvas.getBoundingClientRect(),ctx=canvas.getContext('2d'),w=box.width,h=box.height,p=24;canvas.width=w*ratio;canvas.height=h*ratio;ctx.scale(ratio,ratio);const min=Math.min(...values)*.92,max=Math.max(...values)*1.04||1;ctx.strokeStyle='rgba(80,148,196,.16)';for(let i=0;i<5;i++){const y=p+(h-2*p)*i/4;ctx.beginPath();ctx.moveTo(p,y);ctx.lineTo(w-p,y);ctx.stroke()}const pts=values.map((v,i)=>({x:p+(w-2*p)*(values.length===1?.5:i/(values.length-1)),y:p+(h-2*p)*(1-(v-min)/(max-min||1))}));const grad=ctx.createLinearGradient(0,p,0,h);grad.addColorStop(0,'rgba(24,168,255,.35)');grad.addColorStop(1,'rgba(24,168,255,0)');ctx.beginPath();ctx.moveTo(pts[0].x,h-p);pts.forEach(pt=>ctx.lineTo(pt.x,pt.y));ctx.lineTo(pts.at(-1).x,h-p);ctx.closePath();ctx.fillStyle=grad;ctx.fill();ctx.beginPath();pts.forEach((pt,i)=>i?ctx.lineTo(pt.x,pt.y):ctx.moveTo(pt.x,pt.y));ctx.strokeStyle='#2ecbff';ctx.lineWidth=3;ctx.stroke();pts.forEach((pt,i)=>{ctx.beginPath();ctx.arc(pt.x,pt.y,i===pts.length-1?6:3,0,Math.PI*2);ctx.fillStyle=i===pts.length-1?'#fff':'#18a8ff';ctx.fill();ctx.strokeStyle='#18a8ff';ctx.lineWidth=3;ctx.stroke()})}}
